#include "render2.c"

//the one reason to finish this is because it's fast and to the point...
int main (int argc, char *argv[]) {
	Render r; Table t;
	render_init( &r, &t );
	render_map( r, 
	while ( t->zero ) {


	}
		
}
